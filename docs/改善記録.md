# 改善記録

## 2026-05-31

### 方針決定
- 学習記録の正の保存先はSupabase Postgresにする。
- GitHubはコード、仕様、DBマイグレーション、変更履歴の管理に使い、ユーザーの学習記録そのものは保存しない。
- ブラウザのlocalStorageは、オフライン時や同期失敗時の一時保存として残す。
- ログインは、管理者が事前に登録したメールアドレスだけを許可する方針にする。
- 初回利用時のQRコードは「アプリを開くための導線」とし、毎回のログイン手段にはしない。
- 2回目以降は、Supabase Authのセッションが残っていればアプリアイコンやブックマークからそのまま使える形を目指す。

### 実装した改善
- Supabase保存用のDBマイグレーション案を追加した。
- ユーザー本人は自分の学習記録だけ、管理者は全ユーザーの記録を見られるRLS方針を追加した。
- 学習進捗、日別学習記録、プロフィールをSupabaseに保存する設計に更新した。
- アプリ側にSupabase同期処理を追加した。
  - `user_app_state` に現在章、章ごとの進捗、単語カード、アウトプット進捗、リマインド、質問メモを保存する。
  - `daily_learning_records` に日別の学習時間、解答数、正答数、完了章数を保存する。
  - 同期できない場合はlocalStorageに一時保存する。
- ログイン画面をメールアドレス + 6桁コード方式に変更した。
- 管理画面はSupabase上のプロフィール・学習記録を読める場合、サーバー側の全ユーザー記録を優先表示するようにした。
- スマホ利用を前提に、PWA用のmanifest、service worker、ホーム画面アイコンを追加した。
- Supabase Edge Functionでmanifest、service worker、アイコンを配信できるようにした。

### 未完了・次に確認すること
- Supabase本番REST APIで確認したところ、`profiles`、`user_app_state`、`daily_learning_records` はまだ存在しないため、Supabase DashboardでマイグレーションSQLを適用する必要がある。
- 管理者ユーザーの `profiles.role` を `admin` に更新する必要がある。
- Supabase Authのメールテンプレートを、Magic Linkか6桁OTPのどちらで運用するか決める必要がある。
- 管理画面からAuthユーザーを作成するには、service role keyを使うEdge Functionが必要。
- ローカルにはSupabase Access Token、DB接続文字列、DBパスワードが見つからなかったため、DB反映作業には追加情報が必要。

## 2026-06-20

### 方針決定
- アプリ配布用QRコードはスマホ版・パソコン版で分けない。
- 共通URL `https://kiyori015.github.io/lingopilot/` をQRコードにする。
- ユーザーは登録番号とメールアドレスでログインする。
- 登録番号は管理画面でメールアドレスを登録した順に `01`、`02`、`03`...で発行する。

### 実装した改善
- ログイン画面を「登録番号 + メールアドレス」に変更した。
- 管理画面のユーザー登録を、メールアドレス入力・自動採番・案内メール文作成の流れに変更した。
- 登録番号ログイン用のSupabase Edge Function `lingopilot-auth` を追加した。
- 登録番号採番用のマイグレーション `202606200001_registration_number_login.sql` を追加した。
- 共通URL用QRコードを `src/assets/lingopilot-app-qr.png` と `src/assets/lingopilot-app-qr.svg` に追加した。
- 配布手順を `docs/ログイン配布手順.md` に追加した。

### 未完了・次に確認すること
- Supabase Dashboardで新旧マイグレーションを適用する必要がある。
- `lingopilot-auth` Edge Functionをデプロイする必要がある。
- `SUPABASE_SERVICE_ROLE_KEY` と `LINGOPILOT_LOGIN_PEPPER` をEdge Functionのシークレットに設定する必要がある。
- 実際のメール送信はまだ自動送信ではなく、管理画面で作成されたメール文を送る方式。

## 2026-06-20 本番反映準備

### 確認したこと
- Supabase CLIは `npx supabase` で利用できる。
- ローカルにはSupabase Access Tokenが未設定。
- 本番DBへマイグレーションを反映するためのDBパスワードまたはDB接続URLも未設定。

### 追加したもの
- `scripts/deploy-supabase-production.ps1` を追加した。
- 秘密情報をリポジトリに保存せず、環境変数から本番反映できるようにした。

### 必要な情報
- `SUPABASE_ACCESS_TOKEN`
- `SUPABASE_DB_PASSWORD` または `SUPABASE_DB_URL`
- 必要に応じて `SUPABASE_SERVICE_ROLE_KEY`

## 2026-06-20 初期登録ユーザー

### 登録したユーザー
- `01` / `kiyori.satoh@gmail.com`
- `02` / `nyonaha@chiba-u.jp`
- `03` / `satoh@hakuba.jp`

### 反映内容
- アプリの管理画面に初期登録済みユーザーとして表示されるようにした。
- 一覧は `docs/初期登録ユーザー.md` に残した。

### 注意
- 本番Supabase Authへの正式登録は、Supabase本番反映後に別途必要。

## 2026-06-20 学習画面の軽量化

### 実装した改善
- 章画面を長い縦スクロールからステップ切替に変更した。
- ステップは `場面`、`基本`、`文法`、`単語`、`カード`、`聞く`、`練習`、`結果`、`話す` のうち、章に必要なものだけ表示する。
- `前へ` / `次へ` ボタンで進める形にした。
- 基本例文と重要語彙は、まず見える範囲を最大5件に絞った。
- 文法ポイントは最大3件に絞った。
- 文法説明を短くして、カード表示で例文とセットで見られるようにした。

### 追加で検討したいこと
- 各章の「本当に最頻出の3〜5項目」は、現在は既存順の上位を採用している。
- より精密にするなら、章ごとの例文・語彙を人間が最終選定する専用レビューが必要。
- イラスト・画像は、既存の場面イラストをステップの最初に見せる形にした。実写・生成画像を入れる場合は、章ごとに画像方針を決める。

## 2026-06-20 音声ファーストと復習導線

### 方針
- 文字を読むことよりも、「聞いた瞬間に意味がわかること」を最重要にする。
- 文字は答え合わせ・補助・不安なときの確認用に回し、最初の学習動作は音声再生から始める。
- 最終ゴールは、読めることではなく、聞いて理解でき、同じ場面で自分でも言えることにする。
- 場面イメージ、短い日本語の意味、音声の反復を組み合わせ、表現と使用場面を結びつける。

### 実装した改善
- 各章に「聞く」ステップを追加した。
- 基本フレーズを3〜5件に絞り、韓国語文字を最初は隠して、音声と意味から確認できるカードにした。
- 「5フレーズを聞く」「3回リピート」ボタンを追加し、同じフレーズを繰り返し聞けるようにした。
- 音声カードごとに、聞く・文字を見る/隠す・録音・再生を使えるようにした。
- マイページに「今日のおすすめ復習」を追加した。
- 章の復習回数を `1回目 / 2回目 / 3回目...` として記録できるようにした。
- 復習完了後は、翌日・3日後・7日後の間隔で次回復習日を自動設定するようにした。

### 今後さらに良くする案
- Web Speechの自動音声ではなく、韓国語ネイティブ音声を録音または生成して差し替える。
- 章ごとに場面画像を用意し、最初に画像を見てから音声を聞く流れを強める。
- 「聞いて意味を選ぶ」「聞いて同じ場面画像を選ぶ」「聞いてまねして録音する」の順に進む音声中心レッスンにする。
- 管理者側で復習予定日・復習回数・未復習者を一覧できるようにする。
## 2026-06-20 ログイン応急対応

- GitHub Pages 公開版で、Supabase 本番設定が未完了でも試用できるようにログイン fallback を追加した。
- 管理者は `admin / lingo01` でローカル管理者ログインできるようにした。
- 初期登録済みユーザー `01` `02` `03` は、登録番号とメールアドレスの組み合わせでローカルログインできるようにした。
- ローカル fallback で入った場合、学習記録は端末保存を優先し、Supabase 同期は無効にする。

## 2026-06-21 .env 運用追加

- `scripts/deploy-supabase-production.ps1` が `.env` と `.env.local` を自動で読むようにした。
- `.env.example` を追加し、Supabase 本番反映に必要な環境変数のひな形を用意した。
- `.env` 系ファイルは `.gitignore` に追加し、秘密情報が Git に入らないようにした。

## 2026-06-21 Supabase 本番認証の正式化

- Supabase 本番プロジェクト `ieakvpwzhihqttcxegti` に対して、DB マイグレーションと `lingopilot-auth` Edge Function の反映を確認した。
- `.env.local` の `SUPABASE_SERVICE_ROLE_KEY` と `LINGOPILOT_LOGIN_PEPPER` を本番運用向けの値に更新した。
- 管理者ログイン `admin / lingo01` を Supabase Auth の通常経路で通る状態に整えた。
- 初期ユーザー `01 / kiyori.satoh@gmail.com`、`02 / nyonaha@chiba-u.jp`、`03 / satoh@hakuba.jp` を Supabase に正式登録した。
- 3ユーザーとも、登録番号 + メールアドレスで `lingopilot-auth` 経由のログインが成功することを確認した。
- アプリ側のローカル fallback ログイン分岐を削除し、通常の Supabase 認証に一本化した。
- `scripts/deploy-supabase-production.ps1` から、Supabase 管理環境に元から存在する `SUPABASE_SERVICE_ROLE_KEY` の再設定処理を外した。

## 2026-06-21 タイ語版ログイン方式の統一

- `src/thai.html` と `supabase/functions/lingopilot/thai.html` を更新し、タイ語版も `登録番号 + メールアドレス` でログインする方式に統一した。
- タイ語版の管理画面でも、メールアドレス登録から `lingopilot-auth` を使って登録番号を発行できるようにした。

## 2026-07-02 タイ語版の学習仕様を韓国語版へ統一

- 課内を「場面・聞く・基本・文法・単語・カード・聞き取り・練習・結果・話す」の段階表示に変更した。
- 基本フレーズと重要語彙は最大5件、文法ポイントは最大3件に絞った。
- タイ文字を最初は隠し、音声・日本語の意味・録音から学べる音声ファースト練習を追加した。
- 学習済み課の復習回数と次回復習日を記録し、翌日・3日後・7日後のおすすめ復習をマイページへ表示するようにした。
- タイ語版の学習記録をSupabaseへ同期するため、韓国語版とは分離した `thai_user_app_state` と `thai_daily_learning_records` を追加した。
