# 未完了メモ: 本番反映

## 状況

登録番号ログイン方式の実装、QRコード作成、Supabase用マイグレーション、Edge Functionの追加は完了済み。

ただし、Supabase本番環境への反映は未完了。

## できなかったこと

### 1. DBマイグレーションの本番適用

未実行。

対象ファイル:

- `supabase/migrations/202605310001_learning_records.sql`
- `supabase/migrations/202606200001_registration_number_login.sql`

理由:

- Supabase CLIの `SUPABASE_ACCESS_TOKEN` が未設定。
- リモートDBへ反映するための `SUPABASE_DB_PASSWORD` または `SUPABASE_DB_URL` が未設定。

### 2. Edge Function secretsの設定

未実行。

設定が必要な値:

- `SUPABASE_SERVICE_ROLE_KEY`
- `LINGOPILOT_LOGIN_PEPPER`
- `LINGOPILOT_APP_URL=https://kiyori015.github.io/lingopilot/`

理由:

- Supabase CLIで本番プロジェクトに接続するための `SUPABASE_ACCESS_TOKEN` が未設定。
- `SUPABASE_SERVICE_ROLE_KEY` が未取得。

### 3. `lingopilot-auth` Edge Functionのデプロイ

未実行。

対象:

- `supabase/functions/lingopilot-auth/index.ts`

理由:

- Supabase CLIの `SUPABASE_ACCESS_TOKEN` が未設定。

### 4. 本番動作確認

未実行。

確認したいこと:

- `profiles` テーブルが作成されているか。
- `user_app_state` テーブルが作成されているか。
- `daily_learning_records` テーブルが作成されているか。
- 管理画面でメールアドレス登録すると `01`、`02`、`03`...が発行されるか。
- 発行された登録番号とメールアドレスでログインできるか。
- 学習記録がSupabaseに保存されるか。

## 次回必要な情報

以下のどちらかの方法で進める。

### 方法A: CLIで反映する

必要な情報:

- `SUPABASE_ACCESS_TOKEN`
- `SUPABASE_DB_PASSWORD` または `SUPABASE_DB_URL`
- `SUPABASE_SERVICE_ROLE_KEY`

実行予定スクリプト:

```powershell
.\scripts\deploy-supabase-production.ps1
```

### 方法B: Supabase Dashboardで手動反映する

必要な操作:

1. SQL Editorで `supabase/migrations/202605310001_learning_records.sql` を実行する。
2. SQL Editorで `supabase/migrations/202606200001_registration_number_login.sql` を実行する。
3. Edge Function `lingopilot-auth` をデプロイする。
4. Function secretsを設定する。
5. 管理画面でテストユーザーを登録する。
6. 登録番号とメールアドレスでログイン確認する。

## 注意

- 秘密情報はリポジトリに保存しない。
- Access Token、DB Password、Service Role Keyはチャットやドキュメントに貼らない。
- PowerShellの環境変数として一時的に設定してから実行するのが安全。
